name: workflow-context

on: ["push"]

jobs:
  my-context:
    runs-on: ubuntu-latest
    steps:
      - name: "First Step"
        run: echo "The action is $MY_ACTION"
        env:
          MY_ACTION: ${{ github.action }}
        