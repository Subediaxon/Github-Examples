name: "Workflow Commands"

on:
  push:
    branches:
        - main
jobs: 
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: "group logging"
        runs: |
          echo "::group::MY group message"
          echo "Msg 1"
          echo "Msg2"
          echo "::endgroup::"